-- Tạo giao diện thông báo cập nhật
local ScreenGui = Instance.new("ScreenGui")
local Background = Instance.new("Frame")
local TitleLabel = Instance.new("TextLabel")
local ContactLabel = Instance.new("TextLabel")

-- Cấu hình ScreenGui (Hiển thị đè lên game)
ScreenGui.Parent = game.CoreGui -- Hoặc game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- Nền đen phủ kín toàn bộ màn hình
Background.Name = "Background"
Background.Parent = ScreenGui
Background.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Màu đen
Background.Size = UDim2.new(1, 0, 1, 0)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.BorderSizePixel = 0
Background.ZIndex = 99999

-- Chữ thông báo ở chính giữa màn hình
TitleLabel.Name = "TitleLabel"
TitleLabel.Parent = Background
TitleLabel.BackgroundTransparency = 1
TitleLabel.AnchorPoint = Vector2.new(0.5, 0.5)
TitleLabel.Position = UDim2.new(0.5, 0, 0.45, 0)
TitleLabel.Size = UDim2.new(0.8, 0, 0.1, 0)
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.Text = "ĐÃ CÓ BẢN SCRIPT MỚI!"
TitleLabel.TextColor3 = Color3.fromRGB(255, 50, 50) -- Màu đỏ nổi bật (hoặc đổi thành 255,255,255 nếu muốn màu trắng)
TitleLabel.TextSize = 45
TitleLabel.TextWrapped = true

-- Chữ liên hệ nằm ngay ở phía dưới
ContactLabel.Name = "ContactLabel"
ContactLabel.Parent = Background
ContactLabel.BackgroundTransparency = 1
ContactLabel.AnchorPoint = Vector2.new(0.5, 0.5)
ContactLabel.Position = UDim2.new(0.5, 0, 0.55, 0)
ContactLabel.Size = UDim2.new(0.8, 0, 0.08, 0)
ContactLabel.Font = Enum.Font.SourceSansItalic
ContactLabel.Text = "Để lấy bản mới, vui lòng liên hệ: Nguyễn Văn Nhật"
ContactLabel.TextColor3 = Color3.fromRGB(200, 200, 200) -- Màu xám trắng
ContactLabel.TextSize = 25
ContactLabel.TextWrapped = true
r3 = Color3.fromRGB(25, 30, 35), TextColor3 = Color3.fromRGB(200, 200, 200)}):Play()
                TweenService:Create(bStroke, TweenInfo.new(0.1), {Color = Color3.fromRGB(60, 60, 70)}):Play()
            end)
            return btn
        end
        
        local GetKeyBtn = createBtn("GetKeyBtn", "GET KEY LINK", UDim2.new(0.05, 0, 0, 134))
        local CheckKeyBtn = createBtn("CheckKeyBtn", "CHECK ACCESS KEY", UDim2.new(1, -176, 0, 134))
        
        CheckKeyBtn.MouseButton1Down:Connect(function() 
            local inputKey = TextBox.Text
            local currentHWID = tostring(fGetHwid())

            if inputKey ~= "" then
                if inputKey == adminKey then
                    if currentHWID == adminHWID then
                        StatusLine.Text = ">> ADMIN APPROVED. INJECTING VIP..."
                        StatusLine.TextColor3 = NeonColor
                        task.wait(0.8)
                        ScreenGui:Destroy()
                        loadstring(game:HttpGet("https://raw.githubusercontent.com/Fluxyyy333/HoshiOnTop/main/loader.lua"))()
                        return
                    else
                        StatusLine.Text = ">> WARNING: UNAUTHORIZED DEVICE!"
                        StatusLine.TextColor3 = Color3.fromRGB(255, 80, 80)
                        return
                    end
                end
                
                StatusLine.Text = "> CONTACTING PLATO REPOSITORY..."
                StatusLine.TextColor3 = Color3.fromRGB(255, 200, 0)
                local Verify = verifyKey(inputKey)
                if Verify then
                    StatusLine.Text = ">> SUCCESS. LAUNCHING SCRIPT..."
                    StatusLine.TextColor3 = NeonColor
                    task.wait(0.8)
                    ScreenGui:Destroy()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/Fluxyyy333/HoshiOnTop/main/loader.lua"))()
                else
                    StatusLine.Text = ">> ACCESS DENIED: INVALID KEY."
                    StatusLine.TextColor3 = Color3.fromRGB(255, 80, 80)
                end 
            end 
        end)
        
        GetKeyBtn.MouseButton1Down:Connect(function() 
            StatusLine.Text = "> GENERATING SECURE GATEWAY..."
            StatusLine.TextColor3 = Color3.fromRGB(255, 200, 0)
            local success, link = cacheLink();
            if success and link ~= "" then
                fSetClipboard(link);
                StatusLine.Text = ">> DECRYPTED LINK COPIED."
                StatusLine.TextColor3 = NeonColor
            else
                local backupLink = "https://gateway.platoboost.net/a/" .. fToString(service) .. "?id=" .. lDigest(fGetHwid())
                fSetClipboard(backupLink);
                StatusLine.Text = ">> BACKUP LINK INJECTED."
                StatusLine.TextColor3 = NeonColor
            end
        end)
        
        ExitBtn.MouseButton1Down:Connect(function() ScreenGui:Destroy() end)
    end)
    
    if not mainSuccess then
        warn("[NHẬT HUB CRASH LOG]: " .. tostring(mainError))
    end
end)
